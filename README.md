# `treebeard`
Soft Decision Trees for Next-Gen Trigger at CMS.
  
This repo has two parts:

1. **SDT implementation** — data preprocessing, dataloader construction, model architecture, training, Python-side quantisation studies for a Soft Decision Tree ([Frosst & Hinton 2017](https://arxiv.org/abs/1711.09784)) trained on CMS L1T primitives.
2. **High Level Synthesis** — conversion to Vitis HLS firmware using [`conifer`](https://github.com/thesps/conifer)'s existing single-tree hard decision tree backend (specifically using [`ydf`](https://github.com/google/yggdrasil-decision-forests)'s Gradient Boosted Decision Tree), adapted into a Soft Decision Tree architecture.  

## Repo Layout
- `scripts/` — shell scripts to submit model training jobs 
- `treebeard/` — core SDT package implementation. 
    - `models/` — `SDT` architecture, `DeepSetsTeacher` (distillation teacher) 
    - `data/` — dataloaders and fixed-point quantisation utilities
    - `quant/` — numpy/Fxp simulation of HLS `ap_fixed` inference for SDT
    - `analysis/` — quantisation sweeps and model evaluation scripts
    - `train_sdt.py` — main training point, run as `python -m treebeard.train_sdt` from repo root
- `jupyter/` — date preprocessing, dataloader construction, SDT architecture studies, Python-side fixed-point quantisation studies.
- `conifering/` — C++ and HLS backend generated by `conifer`. Single YDF-trained decision tree → conifer → Xilinx HLS. Starting point for SDT conversion to HLS.

## Acknowledgements
Original SDT formulation from Frosst & Hinton, [*Distilling a Neural Network Into a Soft Decision Tree*](https://arxiv.org/abs/1711.09784). Core SDT implementation builds on [Soft-Decision-Tree-Feature-Learning](https://github.com/Ethan-Shapiro/Soft-Decision-Tree-Feature-Learning) by Ethan Shapiro. DFS node-indexing convention and HLS `Tree` struct patterns adapted from [conifer](https://github.com/thesps/conifer).
